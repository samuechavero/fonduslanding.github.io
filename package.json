{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "homepage": "https://samuechavero.github.io/fonduslanding.github.io/",
  "scripts": {
    "dev": "pnpm --filter @workspace/fondus-cotizacion run dev",
    "build": "pnpm --filter @workspace/fondus-cotizacion run build && rm -rf dist && cp -r artifacts/fondus-cotizacion/dist dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -t",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
